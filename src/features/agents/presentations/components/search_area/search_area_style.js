import { StyleSheet } from 'react-native';


export const SearchAreaStyle = StyleSheet.create({

    searchInput: {
        marginTop: 16,
        backgroundColor: '#222',
        color: '#fff',
        padding: 10,
        borderRadius: 8,
        marginBottom: 12,
    },
});